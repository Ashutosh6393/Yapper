// Barrel for @yapper/schemas — the single source of truth for cross-boundary
// (api <-> web <-> socket) data shapes. Schemas are authored in slice 09b;
// 09a establishes the package with the shared `permission` primitive.
export * from "./common";
