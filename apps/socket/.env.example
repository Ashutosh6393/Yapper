SOCKET_PORT=1234
DATABASE_URL=postgres://yapper:yapper@localhost:5432/yapper
REDIS_URL=redis://localhost:6379
# Better Auth base URL (the api origin): issuer/audience + host of the JWKS endpoint the
# handshake JWT is verified against. Optionally override AUTH_JWKS_URL directly.
BETTER_AUTH_URL=http://localhost:4000
