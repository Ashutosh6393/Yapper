NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SOCKET_URL=ws://localhost:1234
# Local-first sync engine (specs 14-23): 1 = instant Dexie reads + optimistic writes + poke/pull
# freshness; 0 or unset = the legacy TanStack Query path (network round-trip per action).
NEXT_PUBLIC_SYNC_ENGINE=1
