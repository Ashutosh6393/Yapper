# 05 · Collaboration & Cursors — Implementation

## Status: not-started

## Completed

## In Progress

## Blocked
- Requires slice 04.

## Next Steps
1. Add `extension-redis` to `socket` (REDIS_URL).
2. Server-authoritative `{userId,name,color}` in awareness.
3. web `CollaborationCursor` wiring (remote caret + selection labels).
4. Presence list from awareness states.
5. Two-instance fanout manual test.

## Session Notes
