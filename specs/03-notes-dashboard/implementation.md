# 03 · Notes & Dashboard — Implementation

## Status: not-started

## Completed

## In Progress

## Blocked
- Requires slice 02 (auth/session middleware).

## Next Steps
1. `requireAuth` middleware (session → `req.userId`).
2. `POST /api/notes` + `GET /api/notes`.
3. `GET/DELETE /api/notes/:id` with ownership guard.
4. web dashboard (list + create + empty state).
5. web `/notes/:id` shell + delete.

## Session Notes
