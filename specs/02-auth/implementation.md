# 02 · Auth — Implementation

## Status: not-started

## Completed

## In Progress

## Blocked
- Requires slices 00 + 01.

## Next Steps
1. `packages/auth` Better Auth instance (providers + Drizzle adapter + jwt plugin).
2. Generate Better Auth tables + migrate; add FK constraints to `user`.
3. Mount `/api/auth/*` + CORS in `api`.
4. web auth client + `/login` (Google/GitHub).
5. Gate `/dashboard` + `/logout`.
6. `verifyJwt` + JWKS + unit test.

## Session Notes
