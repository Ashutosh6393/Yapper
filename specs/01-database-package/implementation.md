# 01 · Database Package — Implementation

## Status: not-started

## Completed

## In Progress

## Blocked
- Requires slice 00 (Compose Postgres + workspace tooling) done.

## Next Steps
1. `packages/db/package.json` + tsconfig.
2. `src/schema.ts` (enums + note, note_doc, note_collaborator).
3. `src/client.ts` from `DATABASE_URL`.
4. `drizzle.config.ts` + `db:generate` / `db:migrate` scripts.
5. Smoke test: insert + select a note.

## Session Notes
